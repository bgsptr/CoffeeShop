name: Deploy to Production

on:
  push:
    branches:
      - main

jobs:
  deployment:
    uses: ./.github/workflows/deploy-cloudrun.yml@main
    with:
      environment: production
      tag: ${{ github.sha }}
