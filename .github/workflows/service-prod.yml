name: Cloud Run Deploy Environment

on:
  push:
    branch: main

jobs:
  deploy-prod:
    uses: ./.github/workflows/deploy-cloudrun.yml@main
    with:
      environment: production
      tag: ${{ github.sha }}